// A placeholder tells the compiler to continue typechecking and assume
// that this will be 'filled in' later. If it isn't, its a compilation error.
//
// Generally, this will be created by a metaprogram, in our case build.jai.
#placeholder IS_DEBUG;
